node_modules/
dist/
.turbo/
data/
artifacts/
package-lock.json

# 레거시 스택과 그 시절 도구 — 비교 기준이라 형식을 건드리지 않는다.
#
# 앞에 슬래시를 붙여 저장소 루트로 못 박는다. 슬래시가 없으면 같은 이름의
# 하위 디렉터리까지 걸린다 — 실제로 `server/` 하나가 `apps/server/` 를
# 통째로 서식 검사에서 빼먹고 있었다.
/legacy/
/test/
/tools/

# 레거시 public/css/style.css 를 한 글자도 안 바꾸고 복사한 것이다.
# 포맷을 손대면 원본과 diff 가 안 맞아 "그대로 옮겼다" 를 확인할 수 없다
# (apps/client/src/features/ui/Shell.spec.tsx 가 원본과 대조한다).
apps/client/src/styles/style.css
