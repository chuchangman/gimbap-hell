node_modules/
dist/
.turbo/
data/
artifacts/
package-lock.json

# 에셋 생성기 — 결과물(GLB)이 중요하지 소스 형식은 아니다.
#
# 앞에 슬래시를 붙여 저장소 루트로 못 박는다. 슬래시가 없으면 같은 이름의
# 하위 디렉터리까지 걸린다 — 예전에 `server/` 하나가 `apps/server/` 를
# 통째로 서식 검사에서 빼먹은 적이 있다.
/tools/

# 손으로 맞춘 CSS 618줄. 포맷터가 재배치하면 어디가 바뀌었는지 못 읽는다.
apps/client/src/styles/style.css
