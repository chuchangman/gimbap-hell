# Render Blueprint — 이 파일이 있으면 Render 가 설정을 자동으로 읽는다.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: gimbap-hell
    runtime: node
    plan: free
    region: singapore # 한국에서 가장 가까운 무료 리전
    # 서버만으로는 화면이 없다 — 클라이언트를 함께 빌드해 dist 를 만든다.
    buildCommand: npm ci && npm run build
    startCommand: npm start # = node apps/server/dist/main.js
    healthCheckPath: /health # HealthController 가 제공하는 엔드포인트
    # main 에 푸시하면 바로 배포된다.
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: '22.12.0' # package.json 의 engines 와 같아야 한다

      # 정적 파일 루트. 기본값과 같지만 명시해 둔다 — 빌드 산출물이
      # 어디서 나가는지가 배포에서 가장 헷갈리는 지점이다.
      - key: GIMBAP_PUBLIC_ROOT
        value: apps/client/dist

      # ── 가게 랭킹 저장소 ─────────────────────────────────────
      # 무료 플랜은 파일 시스템이 휘발성이라 Upstash 에 둔다.
      # URL·토큰을 비워두면 서버는 파일 모드로 돌아가고, 잠들 때마다 초기화된다.
      # sync: false 는 "값을 blueprint 에 적지 말고 대시보드에서 받아라" 라는 뜻.
      - key: UPSTASH_REDIS_REST_URL
        sync: false
      - key: UPSTASH_REDIS_REST_TOKEN
        sync: false

      # 랭킹을 비우려면 뒤 번호를 올린다 (v2 → v3 …).
      # 지우는 게 아니라 새 키로 옮겨가는 것이라, 값을 되돌리면 옛 기록도 돌아온다.
      - key: GIMBAP_LEADERBOARD_KEY
        value: gimbap:leaderboard:v2
